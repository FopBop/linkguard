[linkguard]
# Example ignore rules used by the `examples/broken` walkthrough.
# Paths/globs skipped entirely during discovery:
exclude = vitepress/*, .docusaurus/*
# Broken *remote* URLs matching these globs never fail the run:
ignore = https://flaky.example/*
